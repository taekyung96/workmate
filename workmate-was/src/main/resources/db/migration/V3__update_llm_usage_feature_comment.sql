-- =============================================================
-- llm_usage.feature 코멘트 갱신 — ASSISTANT(페이지 도우미) 추가
--
-- 값은 enum 이름이 문자열로 저장되므로 컬럼 타입·제약은 그대로다.
-- V1 의 코멘트에 값 목록이 나열돼 있어 그것만 현행화한다.
-- (적용된 마이그레이션은 수정 금지 — 체크섬 불일치로 기동이 깨진다)
-- =============================================================

COMMENT ON COLUMN public.llm_usage.feature IS
    '호출 기능 — CHAT(채팅)·OCR(영수증)·STT(음성전사)·SUMMARY(회의록요약)·EMBEDDING(가이드임베딩)·ASSISTANT(페이지도우미)';
