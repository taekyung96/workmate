# 운영 환경 — WAS 주소는 OS 환경변수로만 주입
server:
    port: 8080

spring:
    thymeleaf:
        cache: true # 운영에서는 템플릿 캐시 활성화

was:
    base-url: ${WAS_BASE_URL}

app:
    oauth2:
        # WEB 이 SPA 를 직접 서빙하므로 같은 오리진의 상대경로로 돌려보낸다
        success-redirect-uri: /chat
        failure-redirect-uri: /login
